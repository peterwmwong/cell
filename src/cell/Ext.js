// Generated by CoffeeScript 1.4.0

define(['utils'], function(utils) {
  var Ext;
  Ext = function(options) {
    this.options = options != null ? options : {};
    this.getValue = utils.bind(this.getValue, this);
  };
  Ext.prototype = {
    getValue: function(v, callback) {
      callback(typeof v === 'function' ? v() : v);
    },
    run: function(element, view) {
      this.view = view;
      this.func(element, this.options, this.getValue, view);
    }
  };
  Ext.extend = utils.extend;
  return Ext;
});
