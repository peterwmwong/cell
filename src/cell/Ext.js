// Generated by CoffeeScript 1.6.1

define(['util/hash', 'util/type', 'util/fn', 'util/extend', 'cell/util/spy'], function(hash, type, fn, extend, spy) {
  var Ext;
  Ext = function(options) {
    this.options = options != null ? options : {};
  };
  Ext.prototype.watch = function(v, callback) {
    if (type.isF(v)) {
      spy.watch(hash(this.view), fn.b0(v, this), fn.b1(callback, this));
    } else {
      callback.call(this, v);
    }
  };
  Ext.prototype.run = function(element, view) {
    this.view = view;
    this.el = element;
    this.render();
  };
  Ext.extend = extend;
  return Ext;
});
