// Generated by CoffeeScript 1.4.0

define(function() {
  return {
    msie: Number((/msie (\d+)/.exec(navigator.userAgent.toLowerCase()) || [])[1]),
    define: function(o) {
      return function(e, nameOrHash, value) {
        var name, result, _i, _len, _results;
        if (arguments.length === 3) {
          return o.set(e, nameOrHash, value);
        } else if (nameOrHash) {
          if (nameOrHash.constructor === Object) {
            _results = [];
            for (name in nameOrHash) {
              value = nameOrHash[name];
              _results.push(o.set(e, name, value));
            }
            return _results;
          } else if (nameOrHash instanceof Array) {
            result = {};
            for (_i = 0, _len = nameOrHash.length; _i < _len; _i++) {
              name = nameOrHash[_i];
              result[name] = o.get(e, name);
            }
            return result;
          } else {
            return o.get(e, nameOrHash);
          }
        }
      };
    }
  };
});
