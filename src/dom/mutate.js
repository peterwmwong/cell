// Generated by CoffeeScript 1.6.3
define(['cell/dom/data', 'cell/dom/browser'], function(data, browser) {
  var dealloc, toArray;
  toArray = browser.msie < 9 ? function() {
    var el, _i, _len, _results;
    _results = [];
    for (_i = 0, _len = this.length; _i < _len; _i++) {
      el = this[_i];
      _results.push(el);
    }
    return _results;
  } : Array.prototype.slice;
  dealloc = function(element) {
    var children, i, len;
    if (element.nodeType !== 3) {
      data.remove(element);
      children = toArray.call(element.children);
      len = children.length;
      i = -1;
      while (++i < len) {
        dealloc(children[i]);
      }
    }
  };
  return {
    remove: function(element) {
      var parent;
      dealloc(element);
      if (parent = element.parentNode) {
        parent.removeChild(element);
      }
    }
  };
});

/*
//@ sourceMappingURL=mutate.map
*/
