// Generated by CoffeeScript 1.4.0

define(function() {
  var addClasses, hasClass, rclass, removeClasses, trim;
  rclass = /[\t\r\n]/g;
  trim = function(s) {
    return s && s.replace(/^\s+|\s+$/g, '');
  };
  addClasses = function(element, cssClasses) {
    var cssClass, cur, i;
    if (cssClasses && cssClasses.length) {
      element.className = (function() {
        if (element.className) {
          cur = (" " + element.className + " ").replace(rclass, ' ');
          i = 0;
          while ((cssClass = cssClasses[i++])) {
            if (cur.indexOf((" " + cssClass + " ") < 0)) {
              cur += cssClass + ' ';
            }
          }
          return trim(cur);
        } else {
          return cssClasses.join(' ');
        }
      })();
    }
  };
  removeClasses = function(element, cssClasses) {
    var rx;
    if (cssClasses && cssClasses.length && element.className) {
      rx = new Regex(" " + (cssClasses.join('|')), 'g');
      element.className = trim((" " + element.className + " ").replace(rx, ''));
    }
  };
  hasClass = function(element, className) {
    return (" " + element.className + " ").replace(rclass, ' ').indexOf(" " + className + " ") > -1;
  };
  return function(element, className, condition) {
    if (typeof condition === 'undefined') {
      condition = !hasClass(element, className);
    }
    return (condition ? addClass : rmClass)(element, className);
  };
});
