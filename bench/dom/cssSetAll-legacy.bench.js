// Generated by CoffeeScript 1.4.0

define(function(require) {
  var bench;
  bench = require('bench');
  window.domBaseline = require('domBaseline');
  window.domNow = require('domNow');
  return bench.run({
    setup: "var baseline = domBaseline('<div></div>');\nvar now = domNow('<div></div>');",
    tests: {
      baseline: function() {
        return baseline.css({
          margin: '1px',
          padding: '2px',
          color: '#BADA55'
        });
      },
      now: function() {
        return now.cssSetAll({
          margin: '1px',
          padding: '2px',
          color: '#BADA55'
        });
      }
    }
  });
});
